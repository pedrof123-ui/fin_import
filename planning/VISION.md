VISION:

Create an app Next.js frontend for user to enter a ticker symbol and the app downloads the financial statements for that ticker and enters the financials in the financial_statements.duckdb

Use existing project utilities and pipeline to handle backend  data financial data processing

UI should be modern, sleek and dark mode

There should be a text boxes for the user to enter the ticker, number of periods and a drop down to select period type FY or Q 

Then to display the financial statements, the user selects IS, BS or CF from a dropdown menu

Only display the top XBRL mapping concepts. For example: display revenue and not revenue detais. 

Use front-end design plug in